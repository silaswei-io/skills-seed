# Optimize User Workflow

Organize the user-maintained content below into a concise, executable Markdown workflow for future Agents working in the current project.

Project boundary:
- name: {{.Project.Name}}
- root: {{.Project.RootPath}}
- language: {{.Project.Language}}
- mode: {{.Project.Mode}}

Workflow name: {{.Name}}
Mode: {{if .Overwrite}}overwrite{{else if .ExistingContent}}merge{{else}}create{{end}}

{{if .ExistingContent}}Existing workflow content:
```markdown
{{.ExistingContent}}
```

{{end}}New workflow content:
```markdown
{{.Content}}
```

Speed and precision:
- Prefer executable clarity over expansion. Keep already clear content concise; do not force irrelevant sections.
- Inspect the repository only when a named path/symbol/command needs clarification; stay inside the project root and stop once clarified.
- If conflict remains, mark `To confirm` instead of guessing or surveying broadly.

Requirements:
- Preserve the user's intent, ordering, conditions, permissions, prohibitions, commands, paths, risks, and acceptance signals.
- In merge mode, preserve valid existing content, incorporate the new content, and deduplicate only equivalent wording. Do not silently drop steps or choose one side of a conflict.
- In overwrite mode, use only the new content. Do not restore steps from an earlier version.
- You may inspect the repository read-only when the content names a path, symbol, command, or project concept that needs clarification. Stay within the project root and do not perform a broad survey.
- Use repository evidence only to make the supplied workflow clearer and correctly routed. Do not invent commands, environments, services, tools, steps, guarantees, or deployment targets.
- Never turn a descriptive example into a required step or an available command into permission to execute it.
- Use headings and ordered steps only where they improve execution. Keep already clear content concise; do not force irrelevant sections.
- If inputs conflict or information remains unresolved, preserve both positions and mark the point as `To confirm` instead of guessing.
- Also return optional routing metadata. `summary` is one short sentence describing the task goal this workflow serves; `route_terms` is a small list of concrete natural-language signals from the supplied inputs (resources, actions, states, task types, paths, or external dependencies).
- Routing metadata is navigation only and never grants command permission. Do not invent terms, commands, environments, guarantees, or workflow scope that are absent from the supplied inputs. Keep the list small and specific; omit metadata when the inputs do not support it.
- Return the complete replacement Markdown body. Do not add commentary outside the result.

Return JSON only.

{{ jsonContract "OptimizeWorkflowOutput" }}
