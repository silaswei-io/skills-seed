# Plan Learning Packs

This is the short planning conversation for current-code learning. Build a high-recall set of self-contained evidence packs for the files listed below. Prefer preserving source-backed knowledge boundaries over minimizing the number of packs.

Project:
- name: {{.ProjectName}}
- path: {{.RootPath}}
- language: {{.Language}}
- learning mode: {{.LearningMode}}
- learning scope: {{.LearningScope}}

Learning strategy guidance:
- `fast`: plan compact packs, but still preserve clear source-backed knowledge lanes.
- `normal`: balance coverage and cost; keep distinct routeable product, interaction, resource, contract, or implementation boundaries.
- `deep`: preserve more source-backed local boundaries when they are useful for future implementation decisions.
- The mode changes prioritization only. It is not a fixed count, file budget, or permission to invent weak packs.

Learning scope guidance:
- `domain`: prefer broader product/domain packs when files share the same long-lived responsibility.
- `flow`: prefer packs around user/system workflows, resource actions, external-system responsibilities, state transitions, and policies.
- `module`: prefer finer packs around modules, components, extensions, interfaces/contracts, adapters, or generated-source boundaries when those boundaries drive future edits.
- The scope is a planning lens, not a hard taxonomy. Source evidence and future decision value decide the final pack shape.

Input files are available at `{{.FocusPathsPath}}` ({{.FocusPathCount}} paths).
{{- if .StructuralContextPath}}
Structural context is available at `{{.StructuralContextPath}}`; use it only for entry, symbol, import, call, dependency, and ownership clues.
{{- end}}
{{- if .UserContextPath}}
User context is available at `{{.UserContextPath}}`; use it only to interpret intent.
{{- end}}

Planning rules:
- Each returned focus is an evidence pack, not a permanent project partition and not durable taxonomy.
- Every evidence pack must be self-contained enough for a later isolated pack-analysis conversation: name the product/domain/interaction/technical route terms, entry paths, related paths, and a precise scope reason.
- Do not plan a directory inventory. Group files that are likely to contain knowledge with future implementation, routing, validation, reuse, reusable capability-entry selection, debugging, or agent navigation value.
- Preserve these knowledge lanes when present in the input: product/domain behavior, UI or command actions, interface/contract shape, error handling, validation/testing, state or storage behavior, configuration, framework/language/runtime integration, generated-source boundaries, security-sensitive handling, operational lifecycle, jobs/tasks, adapters, and reusable utilities.
- Before grouping, scan for decision-bearing cues: actors, users, resources/entities, operations/capabilities, verified callable or declarative entries, states/phases, policies/rules, permissions, audit records, configuration gates, external systems, protocols/contracts, generated source inputs, framework/language/runtime usage conventions, jobs/tasks, operational lifecycle paths, exceptions, and compensation behavior.
- Do not let central framework, startup, generated wrapper, or interception paths crowd out decision-bearing product/domain or code-pattern paths. Include separate packs for materially different boundaries even when they share the same entry, component, service, adapter, storage, extension, or generated-code shape.
- Plan horizontal packs when the input contains reusable cross-cutting repository behavior such as interface/request/response/message contracts, error taxonomy, validation commands, utility helpers, reusable entry families, generated-source files, configuration conventions, logging/audit conventions, state/storage construction, or rendering/interaction conventions. These packs are useful even when they are not a product subdomain.
- Include only entry and related paths from the provided file list or structural context. Do not invent files and do not expand into unlisted directories.
- Merge files into one pack only when a future request would naturally modify them together and the same source boundary explains the behavior.
- Split packs when route terms, state/data boundaries, authorization/audit semantics, external dependency contracts, operational risk, or change triggers are materially different.
- If the input is a small diff-oriented set, prefer compact packs anchored to the changed files instead of planning a broad project learning run.
- Use deterministic ordering: business routeability first, then changed/source entry importance, then path vocabulary, then lexicographic path order.

Pre-output self-check:
- Every provided file that should be learned is covered by at least one pack entry or related path.
- No pack is only a vague directory, framework, or layer summary.
- No pack exists only to inventory a language, framework, or module when no future decision value is visible.
- No two packs are duplicates with different names.
- Pack IDs and names are stable, routeable, and do not use sequence numbers as meaning.

Return JSON only.

{{ jsonContract "PlanLearningAgendaOutput" }}
