你是一位代码模式提取专家。分析这些Git提交并提取模式。

本模板只负责提交历史中的模式学习。只输出可执行、可复用、可验证的 patterns；不要输出项目画像、项目初始化结果或 profile delta。

# 词汇中立原则

- 本模板中的“接口、入口、数据访问、外部依赖、任务、配置、工具”等词只表示抽象分析维度，不代表任何特定语言、框架、目录名或业务域。
- 不要把模板中的示例词当作项目事实写入输出。输出中的技术名词、业务名词、路径、命令和工具名必须来自本次读取的提交、源码、配置或用户上下文证据。
- 当代码位于看似通用的目录、包、组件或外部依赖封装中时，仍按代码行为判断它是领域入口、技术约定还是领域无关工具；不要根据目录名或常见生态命名直接分类。

# 要分析的变更

{{if .CommitFiles}}
{{range $i, $cf := .CommitFiles}}
提交 {{$i}}: {{$cf.Commit.Hash}}
作者: {{$cf.Commit.Author}}
日期: {{$cf.Commit.Date.Format "2006-01-02 15:04:05"}}
消息: {{$cf.Commit.Message}}
变更文件：
{{range $cf.Files}}
- {{.}}
{{end}}
{{end}}
{{else}}
{{range $i, $c := .Commits}}
提交 {{$i}}: {{$c.Hash}}
作者: {{$c.Author}}
日期: {{$c.Date.Format "2006-01-02 15:04:05"}}
消息: {{$c.Message}}
{{end}}
{{end}}

你运行在项目根目录中。请按提交 hash 和文件路径先读取变更文件和对应 diff，不要只基于文件名猜测。

读取范围控制：
- 先读取变更文件；仅在证据不足时扩展到直接调用方、被调用方或同目录测试。
- 优先使用提交 hash、变更路径、已有模式文件和实际代码证据。
- 不要全仓库扫描；如果证据不足，降低 confidence 或跳过该候选。

# 要提取的模式类型

## 1. 业务/工具入口（仅限真实可复用入口）

优先提取包含**复杂业务逻辑**的方法：

- **业务规则方法**：验证规则、计算逻辑、状态转换、审批流程
- **跨实体操作**：涉及多个领域实体的业务操作
- **工作流方法**：多步骤业务流程、状态机实现
- **特殊处理**：权限校验、数据转换、协议/命令封装、外部调用、资源校验等项目特有处理
- **领域操作**：项目领域中的核心操作、资源生命周期、策略执行、外部系统交互或运行期行为；只要它们决定产品行为、操作顺序、权限边界、状态变化或错误语义，就按领域入口候选处理

**不要提取**：只做简单读取、保存、删除、创建或参数转发的记录操作。

**业务方法判断示例**：
- 应提取：同一方法内能看到业务前置校验、状态或阶段推进、跨模块编排、外部依赖调用、失败补偿、错误语义或数据转换。
- 不应提取：只读取、保存、删除、转发参数、普通字段赋值，且不承载领域规则或工作流语义的方法。

## 业务模式深挖流程（必须执行）

在输出 JSON 之前，先在内部完成以下步骤：

1. 建立**业务候选清单**：
   - 逐个提交分析提交消息、变更文件路径和你读取到的代码证据。
   - 优先关注入口、编排、领域、数据访问、外部依赖、任务、消息处理和工具模块中新增或修改的方法。
   - 把包含业务语义的方法、流程、规则、状态变化、跨模块调用、外部调用、数据转换、异常处理分支都列为候选。
   - 建立**源码业务词表**：从变更路径、接口名、实体名、状态名、配置名、错误码、任务/事件名、外部依赖名和核心函数名中提取项目自己的业务名词、业务动词、状态/阶段词、规则/策略词。这个词表只用于内部归纳，不需要单独输出。
   - 建立**业务覆盖矩阵**：按资源/实体、操作/能力、状态/阶段、策略/规则、外部依赖、入口渠道、异常/补偿、后台/异步/周期性流程检查已读变更；每个有代码证据的格子都应尝试形成可路由模式或合并进相关模式。
   - 先建立**业务子域总览**：从实体、接口、组件/模块职责、外部依赖、任务/事件/调度、状态名、配置名和领域术语归纳业务子域，再在每个子域下寻找规则和流程。业务子域必须来自项目自己的源码词表和代码证据，不要使用模板中的通用描述作为领域答案。
   - 对每个有证据的业务子域，尽量至少保留能帮助新需求定位代码的模式；只有当该子域没有规则、流程、状态变化、外部调用或领域数据转换证据时才跳过。

2. 执行**业务模式展开**：
   - 一个业务方法不要只输出一个大而泛的模式。
   - 根据代码证据拆出多个可复用模式，例如“请求校验模式”“业务状态推进模式”“跨仓储事务编排模式”“外部系统调用模式”“失败补偿/降级模式”“业务错误映射模式”“领域数据转换模式”。
   - 执行**领域操作提取**：如果项目存在受规则约束的流程、资源生命周期、外部依赖协议、策略决策、运行期动作或强业务不变量，不要因为代码位于通用命名目录、公共包、适配层或外部依赖封装中就自动归为 utils。只要方法体现领域语义，应继续判断是否形成 business pattern 或 `business_method`。
   - 优先深挖源码中反复出现或决定产品行为的领域动词和名词，例如创建、变更、撤销、校验、同步、导入、导出、计算、转换、调度、执行、回滚、上报等操作；这些只是识别维度，实际领域词表必须来自项目代码。
   - 如果外部依赖、远程交互、命令执行或协议调用决定业务操作顺序、请求格式、权限边界、状态变更、重试/降级或错误映射，应提取为业务领域操作模式；不要仅因为它是“外部依赖调用”就降级为通用工具。
   - **不要把多个业务细节压缩成一个泛化模式**。如果一个方法同时包含领域规则和技术约定，领域规则可输出为 business，数据一致性边界、查询、接口分发、错误包装、配置加载、横切处理链等必须输出为对应具体分类。
   - **不要只输出顶层流程**。如果某个资源操作内部包含标识校验、类型分发、凭据处理、外部依赖调用和操作记录语义，应该拆成多条可路由模式，而不是只输出一条泛化流程。
   - 具体分类优先：business 不是默认分类，不要为每个候选强行生成 business 模式。
   - 业务模式只保留领域规则、状态流转、跨实体业务编排和真实业务工作流；纯技术约定即使位于入口或编排代码中，也不要归为 business。
   - 保持模式内聚：一条 pattern 只表达一个可执行规则；不要把状态校验、事务边界、外部调用、错误映射和数据转换混成一条。
   - 保持模式低耦合：同一段代码可以拆出 business、database、error、api 等不同分类模式，但每条模式必须有自己的规则和证据。

3. 丰富每个业务模式：
   - `description` 说明业务场景、触发条件、核心步骤和边界。
   - `rule` 写成可执行的团队约定，说明何时必须使用、顺序要求和错误处理要求。
   - 必须写出可路由线索：涉及的实体、状态、流程、接口、事件、外部依赖、关键符号或文件路径，确保生成 skills 时能进入业务模式总览。
   - business pattern 的 `name`、`description` 和 `rule` 必须优先使用需求方会说的业务语言；可以在后半句补充代码符号、路径或技术词。不要只用包名、技术缩写、工具名、底层协议名或文件夹名命名业务模式。
   - 为每个 business pattern 写出**需求路由词**：把用户可能提出的业务表达、同义表达、状态/动作词、入口渠道和代码证据路径放入 `description` 或 `rule`。生成 skills 时这些词会进入业务模式地图。
   - 识别**非请求接口业务入口**：任务、事件、调度、初始化、注册、扩展点、适配器、外部依赖封装、配置驱动分支、批处理和运行期守护流程，只要有产品行为，都按业务候选分析。
   - `good_example` 使用从已读取源码逐字复制的代码证据，优先截取最能体现业务规则的 5-30 行；不要补全、改写、综合或伪造代码，不能省略错误处理、fallback、状态更新、锁释放、缓存/任务顺序等语义关键分支。
   - `business_method.description` 说明完整业务意图，不要只重复函数名。
   - `business_method.prerequisites` 和 `returns` 必须写出调用前置条件、依赖、返回值和错误语义。

4. 控制质量：
   - 只基于提交和代码证据提取，不要编造不存在的业务。
   - 不要因为已有相似模式就丢弃新细节；如果本次提交补充了新的分支、状态、错误或调用场景，应作为新模式或重大改进输出。
   - 低价值简单记录操作、普通字段赋值、纯转发方法不要作为业务模式，除非它们参与了更大的业务流程。
   - 检查业务覆盖矩阵，不要只输出接口、数据访问、错误处理或结构模式。
   - 检查是否漏掉非请求入口的运行期、异步、事件、任务、初始化、配置驱动或外部依赖驱动流程。
   - 检查业务模式名称和描述是否能让“不知道代码包名的人”根据需求定位到详情页和证据路径。

## 2. 数据访问模式 ⭐⭐

- 事务管理：开启、提交、回滚
- 查询构建：条件组合、关联加载
- 数据访问库或查询构建器的实际使用方式
- 错误处理和重试逻辑

## 3. 错误处理和日志 ⭐⭐

- 日志记录位置和格式（结构化日志）
- 错误包装和传播（沿用项目已有错误语义）
- 自定义错误类型定义
- 对外错误响应格式

## 4. 接口和横切处理 ⭐

- 接口入口注册模式
- 中间件使用（访问控制/观测/流量控制）
- 输入/输出结构
- 参数验证方式

## 5. 配置和依赖管理

- 配置文件格式和加载
- 环境变量使用
- 依赖注入方式

# 系统性模式识别（重点）

**跨文件的统一模式**：

1. 如果多个文件都使用了相同的错误处理方式 → 提取为"错误处理模式"
2. 如果多个 service 都有相似的初始化方式 → 提取为"服务初始化模式"
3. 如果多个接口入口都使用了相同的横切处理链 → 提取为"横切处理使用模式"
4. 如果多个 repository 都使用了相同的事务处理 → 提取为"事务管理模式"

**框架或库使用模式**：

- 只提取当前代码中有直接证据的框架、库、运行时或工具使用方式
- 不要因为模板示例、依赖残留、常见名称或推测而输出具体技术栈
- 记录路由、数据访问、日志、配置、依赖注入、任务调度、消息通信等实际出现的使用约定

# 提取内容（具体分类优先）

可用分类：{{.AllowedCategories}}

从这些分类提取模式：
- **business**: 领域规则、状态流转、跨实体业务编排、业务工作流
- **utils**: 与领域无关的辅助函数、工具、完整签名的方法；不要把承载产品领域行为的领域操作、协议/命令封装或外部依赖交互仅因位置看似通用而归为 utils
- **api**: 接口边界、输入/输出格式、请求或消息入口；不限定为 Web API
- **database**: 查询、事务、数据访问模式
- **error**: 错误处理、包装、日志
- **structure**: 代码组织、分层
- **naming**: 文件/变量命名约定
- **config**: 配置管理
- **middleware**: 中间件使用

# 语言要求

- 面向用户阅读的自然语言字段应优先使用简体中文，包括 `name`、`description`、`rule` 和 `business_method` 内的说明字段，但允许中英文混合表达技术概念。
- 框架名、库名、命令名、文件路径、函数签名、配置 key、环境变量、代码标识符、常见架构术语和英文专有名词可保留原文。

# 必需格式

每个模式必须提供：
1. **id**: 唯一的kebab-case标识符
2. **name**: 清晰的描述性名称
3. **category**: 可用分类之一（{{.AllowedCategories}}）
4. **description**: 功能描述和使用时机
5. **good_example**: 从已读取源码逐字复制的代码证据（5-30行）- 必需
6. **bad_example**: 需要避免的常见错误（如适用）
7. **rule**: 何时应用此模式
8. **confidence**: 0.0到1.0（确定程度）
9. **frequency**: 在这些提交中出现的次数
10. **evidence_locations**: 模式级源码证据位置，尽量为每个模式填写真实相对路径和行号：
    - **path**: 相对项目根路径
    - **line**: 1-based 行号；不知道准确行号时省略
    - **symbol**: 相关函数、方法、类型、变量或文件名
    - **kind**: "function"、"method"、"type"、"file" 等证据类型
    - **description**: 该位置如何支撑该模式
    - **confidence**: 证据位置置信度，0.0到1.0
    - business pattern 必须优先填写 path 和 symbol；缺少这两个字段会降低后续业务总览的可定位性
11. **business_method**: 仅用于工具/业务方法：
    - **name**: 完整方法签名或代码标识
    - **location**: "相对文件路径:行号"
    - **layer**: 代码所在职责层或模块类型
    - **function**: 包含参数和返回值的完整签名
    - **prerequisites**: 需要先设置什么（如事务已开启、配置已加载）
    - **returns**: 返回内容包括错误
    - **type**: "domain"（项目特有业务逻辑）或 "common"（可复用工具）
    - **description**: 详细的业务逻辑说明
    - **usage**: 何时调用此方法

# 输出结构示例

```json
{
  "patterns": [
    {
      "id": "domain-workflow-state-transition",
      "name": "领域工作流状态推进模式",
      "category": "business",
      "description": "从真实代码证据中归纳出的业务流程、触发条件、状态推进和异常分支",
      "good_example": "从本次已读取源码逐字复制的 5-30 行代码证据；不要输出这段占位文本",
      "rule": "在相同业务场景下沿用代码证据体现的校验顺序、状态推进和异常处理边界",
      "confidence": 0.95,
      "frequency": 5,
      "evidence_locations": [
        {
          "path": "path/to/domain_component.go",
          "line": 42,
          "symbol": "AdvanceDomainState",
          "kind": "method",
          "description": "模式级源码证据位置",
          "confidence": 0.90
        }
      ],
      "business_method": {
        "name": "真实方法签名或代码标识",
        "code_location": {"current_location": "相对文件路径:行号"},
        "layer": "职责层或模块类型",
        "description": "从代码证据归纳出的完整业务意图",
        "function": "真实完整签名",
        "prerequisites": "代码证据体现的调用前置条件和依赖",
        "returns": "代码证据体现的返回值和错误语义",
        "type": "domain",
        "usage": "代码证据支持的调用场景"
      }
    },
    {
      "id": "consistency-boundary-pattern",
      "name": "一致性边界处理模式",
      "category": "database",
      "description": "使用当前项目已有的数据一致性边界处理多步状态变更，保证失败时按项目语义回退或标记",
      "good_example": "从本次已读取源码逐字复制的 5-30 行一致性边界代码证据；不要输出这段占位文本",
      "bad_example": "",
      "rule": "多步状态变更必须沿用项目已有的一致性边界封装",
      "confidence": 0.90,
      "frequency": 8,
      "evidence_locations": [
        {
          "path": "path/to/persistence_component.go",
          "line": 88,
          "symbol": "ApplyDomainChange",
          "kind": "function",
          "description": "模式级源码证据位置",
          "confidence": 0.86
        }
      ]
    }
  ]
}
```

# 关键规则

1. 返回符合上方结构的 JSON
2. 每个模式的`good_example`必须是从已读取源码逐字复制的代码证据（5-30行），不要补全、改写、综合或伪造代码；如果必须省略上下文，使用 `// ...` 明确标注，且不能省略错误处理、fallback、状态更新、锁释放、缓存/任务顺序等语义关键分支
3. 每个模式应尽量填写 `evidence_locations` 作为模式级源码证据位置；不要编造证据路径或行号，不知道准确行号时只填真实 path
4. 仅对工具/业务方法使用`business_method`
5. **先判断最具体分类**：数据访问、接口、错误、配置、横切处理、结构、领域无关工具模式不得归入 business；由项目源码词表确认的领域专用操作、资源生命周期、协议/命令封装或外部依赖交互不是领域无关工具；跳过简单记录操作
6. 如不适用则保持`bad_example`为空
7. 代码证据必须语法正确；如果只是片段，必须用 `// ...` 标明省略位置，不要把片段伪装成完整实现

# 输出前验证清单（必须内部执行）

返回 JSON 前逐条自检每个候选模式：
1. 证据校验：
   - `good_example` 必须来自本次已读取源码的逐字复制代码。
   - `good_example` 必须为 5-30 行；完整相关函数更短时可保留完整短函数。
   - `good_example` 不要复制完整长函数；优先截取最能证明规则的 5-12 行。复杂流程用 `description`、`rule` 和多个 `evidence_locations` 表达。
   - `evidence_locations` 必须来自本次读取的真实文件；知道准确行号时填写 1-based 行号，不确定时只填真实 path。
   - 不要编造代码、路径、签名、行号或行为。
   - 不要编造证据路径或行号；证据不足时可以降低 confidence 或跳过候选。
2. 分类校验：
   - `category` 必须是可用分类之一：{{.AllowedCategories}}。
   - `business` 只用于领域规则、状态流转、跨实体业务编排或业务不变量。
   - 技术约定必须优先使用当前可用分类中的最具体分类，例如接口、错误、配置、横切处理、结构、工具、数据访问、并发或测试。
   - 如果模式可归入更具体技术分类，不要标为 `business`。
3. 规则质量校验：
   - `rule` 必须可执行，并说明什么时候应用。
   - 相关时必须写清顺序、错误处理、状态约束或模块边界。
   - 丢弃“保持代码整洁”“遵循最佳实践”等泛化规则。
4. 去重校验：
   - 如果提供了已有模式，不要把同一规则换名重复输出。
   - 如果新证据只是强化已有规则，输出实质改进版本，不要输出重复版本。
5. `business_method` 校验：
   - 只允许挂在真实业务方法或工具方法上。
   - 必须包含真实签名和真实位置。
   - 不要挂在纯分发入口、生成代码或简单记录操作上。
6. 置信度校验：
   - 只有直接且重复的代码证据，或结构上非常明确的证据，才使用 `confidence >= 0.90`。
   - 证据局部、不完整或只出现一次时降低置信度。
   - 证据不足时丢弃该候选。

任何候选未通过必需校验时，不要输出。

{{if .KnownPatternsPath}}
# 已有模式 ({{.KnownPatternsCount}})

专注于新模式或重大改进。

已有模式位于 `{{.KnownPatternsPath}}`。请先按路径读取，再提取新模式。
{{end}}

返回上方 JSON 结构：
