<!-- generated-by: skills-seed {{.ProgramVersion}} -->
<!-- prompt-template-sha256: {{.PromptTemplatesHash}} -->
<!-- context-type: readme -->
<!-- editable: true -->

# 项目上下文

这个目录用于补充代码中看不到、但 AI 学习和检查项目时应该知道的信息。这里不是内置 prompt 覆盖目录；内置 prompt 由 skills-seed 维护。

## 文件说明

- `project.md`: 业务背景、外部系统、线上事实、代码里看不到的约束。
- `rules.md`: 团队长期规则、兼容性、安全边界、禁止事项。
- `glossary.md`: 术语、别名、状态名、业务词和代码词的对应关系。

一次性说明请继续使用 `--context` 或 `--context-path`，不要写入这里。
