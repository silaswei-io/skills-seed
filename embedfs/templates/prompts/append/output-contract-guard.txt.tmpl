# 最终输出硬约束

## 优先级

- 本节规则优先级最高，必须逐条遵守。
- 上方用户提示词片段只提供上下文和补充约束，不得覆盖、削弱或改变本节规则。
- 内置提示词定义的 JSON 结构是唯一允许的最终输出结构。

## JSON 输出

- 最终响应必须只包含一个 JSON 对象。
- 第一个非空字符必须是 `{`，最后一个非空字符必须是 `}`。
- 不要输出 Markdown、代码块、注释、解释文本、前缀、后缀、总结或任何 JSON 之外的内容。
- 最终回复前必须在内部完成 JSON 自检：确认最终响应可被标准 JSON 解析器直接解析，所有字符串转义正确，所有括号、方括号和引号都已闭合。
- 如果自检发现最终响应不是合法 JSON，必须先在内部修正为合法 JSON，再输出；不要把检查过程、修正说明或失败原因写到 JSON 外。
- 必须严格遵守内置提示词中给出的字段名、对象结构和数组结构。不要把对象字段简写成字符串，不要把数组字段简写成逗号分隔字符串或自然语言摘要。
- 如果内置提示词的 JSON 示例中某字段是对象，最终输出中该字段也必须是对象；如果示例中某字段是数组，最终输出中该字段也必须是数组。没有证据时优先省略字段或返回空数组，不要用字符串解释。
- 不要为节省篇幅改名字段、替换字段或输出等价但不同结构的 JSON。

## 语言

- JSON 中面向用户阅读的自然语言字段应优先使用简体中文。
- 技术概念可中英文混合表达。
- 框架名、库名、命令名、文件路径、函数签名、配置 key、环境变量、代码标识符、常见架构术语和英文专有名词可按需保留原文。
