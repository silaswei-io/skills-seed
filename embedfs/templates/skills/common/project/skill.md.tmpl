---
name: {{.SkillName}}
description: {{.SkillDescription}}
---

<!-- generated-by: skills-seed {{.ProgramVersion}} -->
<!-- skills-template-sha256: {{.SkillsTemplatesHash}} -->

# {{.ProjectName}} 开发技能

**语言**: {{.Language}}
**最后更新**: {{.LastUpdated}}
**已学习模式**: {{.PatternCount}}

{{if .HasKnowledge}}这是项目入口 skill，只负责把任务路由到最小必要参考并确认约束来源。业务规则、架构细节、代码证据、测试事实和验证命令必须进入对应 reference 后再决策。{{else}}这是项目工作流入口 skill，只负责把匹配任务路由到用户维护的工作流文档。{{end}}

## 使用方式

{{if .HasKnowledge}}
1. 先按任务类型读取最小必要参考，不要一次性读取所有 references。进入任何分类模式后，先检查其中的“相关参考”；任务命中跨边界提示时继续读取对应 reference。
{{if .References.Enabled}}{{if .References.BusinessPatterns}}2. 业务规则、状态流转或跨实体流程：先读 [业务模式地图](./references/patterns/business.md)，再进入命中的详情页。
{{else}}2. 业务规则、状态流转或跨实体流程：检查当前代码并复用附近实现。
{{end}}{{if .References.BusinessMethods}}3. 项目级业务入口、跨模块编排或工具入口：读 [入口方法索引](./references/business-methods.md)。
{{else}}3. 项目级入口：从当前代码和模块入口确认。
{{end}}4. API、合约、适配层、派生产物或配置约定：读 [项目规范](./references/project-spec.md) 和相关分类模式。
{{if .References.KeyModules}}5. 架构、模块边界或依赖关系：读 [项目概览](./references/project-overview.md) 和 [关键模块](./references/modules.md)。
{{else}}5. 架构、模块边界或依赖关系：读 [项目概览](./references/project-overview.md)。
{{end}}6. 验证改动：读 [验证策略](./references/validation.md){{if .References.Testing}}；涉及 Go 代码时再读 [测试矩阵](./references/testing.md){{end}}。
{{else}}2. 本次生成未写入 references；直接读取当前代码，并优先沿用附近已有实现。
{{end}}

只有用户维护或内置的显式规则可作为硬约束；有源码证据的 AI 学习模式用于定位并优先复用已有解决方案，不自动提升为强制规则。references 与当前代码冲突时，以当前代码为准；缺少依据时说明缺口，不要补造项目规则。
{{else}}{{if .WorkflowReferences}}1. 先从“用户工作流”中选择与任务目标最匹配的流程。
2. 读取对应 workflow 文档后再执行，不要根据列表摘要直接操作。
3. 用户本次指令与已保存工作流冲突时，以用户本次指令为准，并说明偏离点。
{{else}}1. 当前没有可用工作流；先读取用户指令和当前项目上下文。
{{end}}

工作流是用户维护的任务流程，可作为执行步骤来源；缺少代码模式和项目画像时，不要补造项目规则。
{{end}}

{{if .WorkflowReferences}}
## 用户工作流

{{range .WorkflowReferences}}- **[{{.Name}}]({{.Path}})**{{if .Description}} - {{.Description}}{{end}}
{{end}}
{{end}}{{if .StateSummaries}}
## 状态与边界速记

{{range .StateSummaries}}- {{.}}
{{end}}
{{end}}
{{if .HasKnowledge}}
## 模式参考

{{if .References.Enabled}}- **[项目规范](./references/project-spec.md)** - 边界、规则、配置和改动触点
- **[项目概览](./references/project-overview.md)** - 架构、技术栈和关键模块
- **[验证策略](./references/validation.md)** - 已学习命令、适用范围和验证缺口
{{if .References.Testing}}- **[测试矩阵](./references/testing.md)** - Go module、测试命令和测试文件证据
{{end}}{{else}}*本次生成未写入 references。*
{{end}}{{if .OverviewReferences}}

### 项目详情

{{range .OverviewReferences}}- **[{{.Title}}]({{.Path}})** - {{.Description}}
{{end}}{{end}}{{if .ReferenceGroups}}
{{range .ReferenceGroups}}
### {{.Title}}

{{range .Items}}- **[{{.Title}}]({{.Path}})** - {{.Description}}
{{end}}{{end}}{{else if and .References.Enabled .HasPatterns}}
*暂未生成分类模式参考。*
{{end}}

## 项目规则

- 引入新模式或抽象前，先检查附近已有代码。
- 如果参考中已有项目特定入口或工具方法，优先复用。
{{if .References.ProjectSpec}}- 派生产物、接口模型、适配层和配置约定以 [项目规范](./references/project-spec.md) 为准。{{else}}- 派生产物、接口模型、适配层和配置约定以当前代码和项目配置为准。{{end}}
- 只有用户维护或内置的显式规则可视为硬约束；AI 学习模式需要按源码复核。
- references 与实际代码不一致时，以当前代码为准。

{{if .KeyInsights}}### 关键洞察

{{range .KeyInsights}}- {{.}}
{{end}}
{{end}}{{if .HasPatterns}}## 模式统计

- **模式总数**: {{.PatternCount}}
- **分类数量**: {{.Categories}}
{{end}}{{end}}

{{if includeSkillsSeedGeneratedNotice}}---

*此技能随每次提交变得更智能。由 [skills-seed](https://github.com/silaswei-io/skills-seed) 生成。*
{{end}}
