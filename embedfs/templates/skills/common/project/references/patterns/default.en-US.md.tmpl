# {{.Category}} Pattern Overview

**Category**: {{.Category}}
**Pattern Count**: {{.PatternCount}}
**Last Updated**: {{.LastUpdated}}

---

## Overview

Use this page to route {{.PatternCount}} source-backed patterns in the {{.Category}} category. Match by the table of contents and headings, then confirm the full boundary in current source; source-learned results are not security or behavior guarantees.

{{if .RelatedReferences}}
## Related References

This page only covers {{.Category}} category conventions. If the task crosses one of these boundaries, continue with the matching reference.

{{range .RelatedReferences}}
- [{{.Title}}]({{.Path}}): {{.Reason}}
{{end}}
{{end}}

{{if gt (len .PatternObjects) 5}}
## Table Of Contents

{{if .ClaimGroups}}{{range .ClaimGroups}}{{range .Claims}}- [{{.Title}}](#{{anchor .Title}})
{{end}}{{end}}{{else}}{{range .PatternObjects}}- [{{.Name}}](#{{anchor .Name}})
{{end}}{{end}}
{{end}}

{{if .PatternObjects}}
{{if .ClaimGroups}}
## User Rules And Reusable Solutions

Reuse by evidence; authoritative rules live in Project Spec. For authentication, authorization, input/output encoding, cryptography, permissions, destructive state changes, or external side effects, inspect the source before relying on the observation; observed handling is not a security guarantee.

{{range .ClaimGroups}}
### {{.Title}}

{{.Description}}

{{range .Claims}}
- **{{.Title}}**{{if .Text}}: {{.Text}}{{end}}
  - Evidence: {{.EvidenceCount}} source files{{if .Scope}}, scope `{{.Scope}}`{{end}}
  - Usage: {{.Usage}}
  {{if .Locations}}- Locations: {{range .Locations}}`{{.}}` {{end}}{{end}}
{{end}}

{{end}}
{{else}}
## Reusable Solutions

Use these patterns to locate and prefer existing implementations. For authentication, authorization, input/output encoding, cryptography, permissions, destructive state changes, or external side effects, inspect the source before relying on the observation; observed handling is not a security guarantee.

{{range .PatternObjects}}
### {{.Name}}

{{if .HighRiskOperational}}- Risk checkpoint: involves irreversible, privileged, state/resource-changing, or externally visible behavior; confirm safeguards from evidence before changing it.
{{end}}
{{if .Description}}{{.Description}}
{{end}}{{if .Rule}}- User rule: {{.Rule}}
{{end}}{{if .EvidenceLocations}}- Locations: {{range .EvidenceLocations}}`{{.DisplayLocation}}` {{end}}
{{end}}
{{end}}
{{end}}

{{end}}

{{if includeSkillsSeedGeneratedNotice}}
---

*This document is automatically generated by skills-seed from {{len .PatternObjects}} patterns*
{{end}}
