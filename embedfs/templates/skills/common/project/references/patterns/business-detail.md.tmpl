# {{.GroupTitle}}

**分类**: {{.Category}}
**模式数量**: {{.PatternCount}}
**最后更新**: {{.LastUpdated}}

{{.GroupSummary.Description}}

本页只反映已学习到且有源码证据的复用点；模式较少的子域仍应按下方证据入口回到当前源码确认完整业务边界。

## 需求匹配

{{if .GroupSummary.IsFallback}}当需求涉及业务规则、状态变化、运行期行为或跨模块流程，但无法从需求词或路径匹配到稳定子域时，读取本页。{{else}}当需求提到 {{if .GroupSummary.Keywords}}{{range .GroupSummary.Keywords}}`{{.}}` {{end}}{{else}}`{{.GroupTitle}}`{{end}}，出现同义业务动作、状态变化、规则策略、入口渠道或外部依赖，或改动路径位于 {{if .GroupSummary.PrimaryPath}}`{{.GroupSummary.PrimaryPath}}`{{else}}本子域相关代码{{end}} 时，优先读取本页。{{end}}

{{if .RelatedReferences}}
## 相关参考

本页只覆盖产品/领域规则；如果任务涉及接口/消息返回、状态/持久化边界或失败语义等跨边界约定，继续读取对应 reference。

{{range .RelatedReferences}}
- [{{.Title}}]({{.Path}}): {{.Reason}}
{{end}}
{{end}}

{{if .PatternObjects}}
{{if gt (len .PatternObjects) 5}}
## 目录

{{range .PatternObjects}}- [{{.Name}}](#{{anchor .Name}})
{{end}}
{{end}}

## 可复用解决方案与证据

按证据定位并优先复用已有实现；权威规则见项目规范。

{{range .PatternObjects}}
### {{.Name}}

{{if .HighRiskOperational}}**风险检查点**: 该模式涉及不可逆、特权、状态/资源变更或外部副作用。仅按已验证入口定位和保留保护边界，不要把危险动作当作普通复用建议。

{{end}}
{{if .Description}}{{if .AllowsHardConstraint}}**说明**: {{.Description}}{{else}}**适用边界**: {{.Description}}{{end}}

{{end}}
{{if .Rule}}**用户规则**: {{.Rule}}{{end}}

{{if .EvidenceLocations}}**已验证源码**

{{range .EvidenceLocations}}- `{{.DisplayLocation}}`{{if .Symbol}} - {{.Symbol}}{{end}}
{{end}}
{{end}}{{if .BusinessMethod}}**已验证入口**: `{{.BusinessMethod.DisplayLocation}}` - {{.BusinessMethod.Name}}
{{end}}

---

{{end}}
{{end}}
