# {{.Category}} 模式概览

**分类**: {{.Category}}
**模式数量**: {{.PatternCount}}
**最后更新**: {{.LastUpdated}}

---

## 概述

本页用于定位 {{.Category}} 分类下的 {{.PatternCount}} 个源码证据模式。先用目录和标题路由，再按位置回到当前源码确认完整边界；源码学习结果不是安全或行为保证。

{{if .RelatedReferences}}
## 相关参考

本页只覆盖 {{.Category}} 分类约定；如果任务跨到下列边界，继续读取对应 reference。

{{range .RelatedReferences}}
- [{{.Title}}]({{.Path}}): {{.Reason}}
{{end}}
{{end}}

{{if gt (len .PatternObjects) 5}}
## 目录

{{if .ClaimGroups}}{{range .ClaimGroups}}{{range .Claims}}- [{{.Title}}](#{{anchor .Title}})
{{end}}{{end}}{{else}}{{range .PatternObjects}}- [{{.Name}}](#{{anchor .Name}})
{{end}}{{end}}
{{end}}

{{if .PatternObjects}}
{{if .ClaimGroups}}
## 用户规则与可复用解决方案

按证据复用；权威规则见项目规范。涉及认证、授权、输入输出编码、加解密、权限、状态破坏或外部副作用时，必须回源码复核，不要把观察到的处理直接理解为安全保证。

{{range .ClaimGroups}}
### {{.Title}}

{{.Description}}

{{range .Claims}}
- **{{.Title}}**{{if .Text}}: {{.Text}}{{end}}
  - 证据：{{.EvidenceCount}} 个源码文件{{if .Scope}}，范围 `{{.Scope}}`{{end}}
  - 使用：{{.Usage}}
  {{if .Locations}}- 位置：{{range .Locations}}`{{.}}` {{end}}{{end}}
{{end}}

{{end}}
{{else}}
## 可复用解决方案

以下模式用于定位并优先复用已有实现。涉及认证、授权、输入输出编码、加解密、权限、状态破坏或外部副作用时，必须回源码复核，不要把观察到的处理直接理解为安全保证。

{{range .PatternObjects}}
### {{.Name}}

{{if .HighRiskOperational}}- 风险检查点：涉及不可逆、特权、状态/资源变更或外部副作用；按证据确认保护边界后再修改。
{{end}}
{{if .Description}}{{.Description}}
{{end}}{{if .Rule}}- 用户规则：{{.Rule}}
{{end}}{{if .EvidenceLocations}}- 位置：{{range .EvidenceLocations}}`{{.DisplayLocation}}` {{end}}
{{end}}
{{end}}
{{end}}

{{end}}

{{if includeSkillsSeedGeneratedNotice}}
---

*此文档由 skills-seed 从 {{len .PatternObjects}} 个模式自动生成*
{{end}}
