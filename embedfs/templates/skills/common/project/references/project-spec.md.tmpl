# {{.ProjectName}} 项目规范

**语言**: {{.Language}}
{{if .ProjectID}}**子项目 ID**: `{{.ProjectID}}`
{{end}}{{if .ScopePath}}**作用范围**: `{{.ScopePath}}`
{{end}}{{if .WorkspaceRole}}**工作区角色**: {{.WorkspaceRole}}
{{end}}**生成时间**: {{.GeneratedAt}}

## 使用方式

- 本规范只保留当前项目的权威约束及同步覆盖；源码学习结论只在分类 patterns reference 中展开。
- 架构地图读 [项目概览](./project-overview.md){{if .References.KeyModules}}，模块详情读 [关键模块](./modules.md){{end}}{{if .References.BusinessMethods}}，能力入口读 [能力入口索引](./business-methods.md){{end}}。
- 权威规则只来自用户维护或内置约束；源码学习结果只作为复用和安全检查入口。
- 显式权威规则优先于当前代码和学习到的 reference；当前代码只能校正陈旧的源码学习摘要，不能推翻或放宽权威约束。

## 目录

- [权威规则（用户/内置）](#权威规则用户内置)
{{if .AuthorityCoverage}}- [权威同步覆盖](#权威同步覆盖)
{{end}}

## 权威规则（用户/内置）

{{if .RuleReferences}}用户维护的完整规则原文：

{{range .RuleReferences}}- [{{.Name}}]({{.Path}}){{if .Paths}}；路径：{{range $i, $path := .Paths}}{{if $i}}、{{end}}`{{$path}}`{{end}}{{else}}；适用于整个项目{{end}}
{{end}}
{{end}}
{{if .EngineeringRules}}{{range .EngineeringRules}}- **{{.Title}}**：{{.Rule}}（来源：`{{.Source}}`{{if .Evidence}}；证据：{{range $i, $path := .Evidence}}{{if $i}}、{{end}}`{{$path}}`{{end}}{{end}}）
{{end}}{{end}}{{if not .EngineeringRules}}{{if not .RuleReferences}}*尚未提取用户或内置权威规则；源码学习模式见分类 patterns references。*
{{end}}{{end}}
{{if .AuthorityCoverage}}## 权威同步覆盖

以下清单用于审阅本次权威同步纳入的输入边界；被纳入不代表每个章节都会产生规则。

{{range .AuthorityCoverage}}- `{{.Source}}`{{if .Sections}}：{{range $i, $section := .Sections}}{{if $i}}、{{end}}{{$section}}{{end}}{{else}}（文件级覆盖）{{end}}
{{end}}
{{end}}
