---
name: skills-seed-cli
description: Use only when the user explicitly asks to run, inspect, or troubleshoot a concrete skills-seed CLI command. Do not use for skills-seed source development, code review, architecture discussion, or general questions.
---

<!-- generated-by: skills-seed -->
<!-- skills-seed-version: {{.Version}} -->
<!-- prompt-templates-sha256: {{.PromptHash}} -->
<!-- skills-templates-sha256: {{.SkillsHash}} -->

# skills-seed CLI Skill

Use this Skill only when the request requires operating the `skills-seed` CLI: running a command, inspecting command output or local CLI state, or diagnosing a concrete command failure.

Do not load it merely because a task mentions `skills-seed`. Source development, code review, architecture design, generated Skill quality review, and general discussion belong to the repository's normal development workflow unless the task also requires a concrete CLI operation.

This is a CLI operation Skill, not a project coding Skill. It teaches how to operate `skills-seed` to build and maintain project/workspace Skills for other repositories.

## Operating Principles

- Prefer a built-in `skills-seed` command over ad hoc scripts.
- Before relying on a flag or default, run `skills-seed <command> --help` because installed versions can differ.
- Run project-scoped commands from the repository root, or from a subdirectory where `skills-seed` can discover `.skills-seed`.
- Use `sync` as the normal high-level workflow: it learns current changes and regenerates Skills when needed.
- Use `learn current` when learning should happen without generation.
- Use `generate skills` when learned data already exists and only files need to be rendered.
- Store durable guidance with `.skills-seed/context/`, `patterns add`, `patterns update`, or `workflow`; use `--context` and `--context-path` only for one run.
- In automation, pass `--no-interactive` where the command supports it.

## Reference Routing

| User Intent | Read |
|---|---|
| Understand runtime state, caches, or persistence | [Operation Model](./references/operation-model.md) |
| Initialize or reset a repository | [Init And Reset](./references/init-reset.md) |
| Add a workspace child project | [Workspace](./references/workspace.md) |
| Learn current code | [Learn](./references/learn.md) or [Sync](./references/sync.md) |
| Regenerate project Skills | [Generate](./references/generate.md) |
| Add durable rules | [Patterns](./references/patterns.md) |
| Add durable workflows | [Workflow](./references/workflow.md) |
| Manage pre-commit integration | [Hook](./references/hook.md) |
| Understand why files were or were not analyzed | [Preview](./references/preview.md) |
| Inspect learned project profile state | [Profile](./references/profile.md) |
| Inspect help, version, logs, or fingerprints | [Log Help Version](./references/log-help-version.md) |
| Install/update/remove this global CLI Skill | [CLI Skill Maintenance](./references/cli-skills.md) |

## Default Workflows

1. First project setup: `skills-seed init`, then `skills-seed sync`.
2. First workspace setup: `skills-seed init --workspace`, then `skills-seed workspace add .`, then `skills-seed sync`.
3. Daily update: `skills-seed sync`.
4. Add a durable rule: `skills-seed patterns add --context "<rule>"`, then `skills-seed generate skills`.
5. Add a durable task process: `skills-seed workflow --name <name> --context "<process>"`, then `skills-seed generate skills`.
6. Debug file selection: `skills-seed preview files --mode current` before learning.
7. Pre-commit workflow: install the hook with `skills-seed hook install`.
