---
name: {{.SkillName}}
description: 修改、审查或扩展 {{.WorkspaceName}} 工作区代码时使用；用于判断影响范围、选择子项目 skill、规划跨项目顺序和并发边界
---

<!-- generated-by: skills-seed {{.ProgramVersion}} -->
<!-- skills-template-sha256: {{.SkillsTemplatesHash}} -->

# {{.WorkspaceName}} 工作区开发技能

这是工作区入口 skill，只负责路由、影响范围和跨项目协作。子项目内部架构、编码约定、业务方法和可执行规则必须进入对应子项目 skill 后再决策。

## 入口流程

1. 先按改动路径把任务归类为单子项目、跨子项目、契约、共享代码、基础设施或未知范围。
2. 读取 [工作区概览](./references/workspace-overview.md)，确认子项目、路径和 skill 位置。
3. 读取受影响子项目的 `SKILL.md`；写代码前必须读取该子项目的 `references/project-spec.md`。
4. 如果涉及多个子项目，读取 [跨项目规则](./references/cross-project-rules.md)，先稳定契约或共享接口，再处理各子项目实现。
5. 如果路径无法匹配任何子项目，先检查 `.skills-seed/config.yaml` 的 `workspace.projects`，不要猜测项目归属。

## 工作区地图

- 子项目数量：{{.ProjectCount}}
{{range .Projects}}
- `{{.ID}}`：`{{.Path}}/**`，角色 `{{.Type}}`，语言 `{{.Language}}`
  {{if .SelfManaged}}- 管理方式：子项目自带 skills-seed，skill 路径按 `{{.SelfManagedConfigPath}}` 的 provider 和输出路径解析{{end}}
{{end}}
{{if .HasContracts}}- 已配置契约路径：见 [工作区概览](./references/workspace-overview.md){{else}}- 未配置契约路径；遇到 `api`、`proto`、`openapi`、`swagger`、`types`、`schema`、`sdk`、`client` 等路径或文件名时，仍按契约变更处理{{end}}
{{if .HasShared}}- 已配置共享路径：见 [工作区概览](./references/workspace-overview.md){{else}}- 未配置共享路径；遇到 `pkg`、`common`、`shared`、`lib`、`utils`、`components` 等跨项目复用路径时，先判断消费者再改动{{end}}
{{if .HasInfra}}- 已配置基础设施路径：见 [工作区概览](./references/workspace-overview.md){{else}}- 未配置基础设施路径；遇到 `deploy`、`docker`、`k8s`、`helm`、`terraform`、`nginx`、`gateway`、`env`、`config` 等路径时，按部署或运行时影响处理{{end}}

## 路由规则

{{range .Projects}}
- `{{.Path}}/**`
  {{if .SelfManaged}}
  - 子项目自带 skills-seed：先读 `{{.SelfManagedConfigPath}}`，该配置解析出的 skill 为 `{{.SkillPath}}`。
  - 再读：`{{.ProjectSpecPath}}`
  {{else}}
  - 先读：`{{.SkillPath}}`
  - 再读：`{{.ProjectSpecPath}}`
  {{end}}
  - 写入边界：默认只写 `{{.Path}}/**`，除非任务明确要求改契约、共享代码或部署配置。
{{end}}

## 影响范围判断

- 只改某个子项目内部实现：读取该子项目 skill；不要读取或修改其他子项目。
- 改 API、请求/响应类型、生成代码输入、SDK、错误码、事件或消息格式：同时读取生产者和消费者子项目 skill。
- 改共享工具、共享类型、公共组件或公共配置：先找所有导入方或打包方，再读取对应子项目 skill。
- 改 Dockerfile、部署脚本、环境变量、网关、反向代理、中间件、CI 或发布配置：读取所有运行时受影响子项目 skill。
- 改根目录配置、工作区脚本或批量命令：先判断脚本覆盖哪些子项目，再按覆盖范围读取 skill。

## 跨项目执行顺序

1. 固定契约或共享接口：先更新接口定义、schema、公共类型或共享函数签名。
2. 更新生产者：后端、服务端、生成源或共享库实现先完成。
3. 更新消费者：前端、客户端、SDK、调用方、适配层和测试跟进。
4. 更新运行时：部署配置、环境变量、网关、中间件和 CI 最后统一对齐。
5. 验证一致性：检查生成物、错误码、类型、配置键、构建命令和测试范围是否匹配。

## 并发边界

- 可以并发：多个互不重叠的子项目内部改动，每个 Agent 只写自己负责的子项目路径。
- 需要串行：契约、共享代码、基础设施、根配置、工作区脚本，以及任何会改变多个子项目输入的文件。
- 并发前必须列出每个 Agent 的写入路径；并发后统一检查接口形状、生成物、配置和测试。
- 如果一个任务同时包含共享接口和子项目实现，先由单个 Agent 完成共享接口，再让子项目并发跟进。

## 参考

- [工作区概览](./references/workspace-overview.md)
- [跨项目规则](./references/cross-project-rules.md)
