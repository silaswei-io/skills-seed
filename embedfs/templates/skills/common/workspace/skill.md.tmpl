---
name: {{.SkillName}}
description: 处理 {{.WorkspaceName}} 工作区中的需求、问题排查、修改、测试或审查时使用，也适用于跨项目接口、共享资源、配置、规则、工作流和影响分析；用于选择子项目 skill 并规划变更顺序与并行边界
---

<!-- generated-by: skills-seed {{.ProgramVersion}} -->
<!-- skills-template-sha256: {{.SkillsTemplatesHash}} -->

# {{.WorkspaceName}} 工作区开发技能

这是工作区入口 skill，只负责判断任务归属、选择子项目 skill，以及识别是否需要跨项目规则。子项目内部架构、编码约定、能力入口和可执行规则必须进入对应子项目 skill 后再决策。

## 使用方式

1. 先读取 [工作区概览](./references/workspace-overview.md)，用路由表确认路径归属、子项目 skill 位置和默认写入边界。
2. 单子项目任务只进入该子项目的 `SKILL.md` 和 `references/project-spec.md`，不要顺手读取或修改其他子项目。
3. 契约、共享代码、基础设施、根目录脚本或多子项目任务，先读取 [跨项目规则](./references/cross-project-rules.md)，再决定执行顺序。
{{if .WorkflowReferences}}4. 命中已沉淀的工作区工作流时，先读取对应工作流，再结合概览和跨项目规则执行。
5. 如果概览、子项目 skill、工作流或配置无法确认产品边界、项目职责或跨项目依赖，先询问用户，不要编造。
{{else}}4. 如果概览、子项目 skill 或配置无法确认产品边界、项目职责或跨项目依赖，先询问用户，不要编造。
{{end}}

{{if .RuleReferences}}执行上述路由前先检查下方“工作区规则”：任务命中明确列出的项目或路径时读取完整规则；未列出的子项目不自动继承该规则。
{{end}}

{{if .WorkflowReferences}}
## 工作区工作流

{{range .WorkflowReferences}}
- [{{.Name}}]({{.Path}}){{if .Summary}}：{{.Summary}}{{else}}{{if .Description}}：{{.Description}}{{end}}{{end}}{{if .RouteTerms}}；适用任务信号：{{range $i, $term := .RouteTerms}}{{if $i}}、{{end}}`{{$term}}`{{end}}{{end}}
{{end}}

{{end}}

{{if .RuleReferences}}
## 工作区规则

{{range .RuleReferences}}- [{{.Name}}]({{.Path}}){{if .Summary}}；{{.Summary}}{{end}}{{if .AffectedProjects}}；项目：{{range $i, $project := .AffectedProjects}}{{if $i}}、{{end}}`{{$project}}`{{end}}{{end}}{{if .Paths}}；路径：{{range $i, $path := .Paths}}{{if $i}}、{{end}}`{{$path}}`{{end}}{{end}}{{if .RouteTerms}}；命中信号：{{range $i, $term := .RouteTerms}}{{if $i}}、{{end}}`{{$term}}`{{end}}{{end}}
{{end}}

{{end}}

## 参考

- [工作区概览](./references/workspace-overview.md)：子项目、路由表、仓库证据支持的依赖和影响范围。
- [跨项目规则](./references/cross-project-rules.md)：多项目改动、契约/共享/运行时配置变更、依赖顺序和并发边界。
