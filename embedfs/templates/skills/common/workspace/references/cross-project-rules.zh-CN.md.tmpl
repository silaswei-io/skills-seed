# 跨项目规则

## 基本原则

- 修改某个子项目代码前，先读取该子项目的 `SKILL.md` 和 `references/project-spec.md`。
- 修改接口契约、共享库或部署配置时，先识别生产者、消费者和运行时影响，再读取对应 skills。
- 不要把单个子项目内部约定提升为全局规则，除非多个子项目共同依赖。
- 工作区规则只处理跨项目协作；子项目内部实现以该子项目 skill 为准。

## 改动顺序

1. 确认路径范围和受影响子项目。
2. 如果涉及 contracts 或 shared，先稳定契约、schema、类型或共享接口。
3. 更新生产者实现，再更新消费者调用、生成代码或适配层。
4. 更新 infra、环境变量、部署配置或中间件规则。
5. 运行受影响项目的测试、生成命令或构建命令。

## 必须同时读取多个 skills 的场景

{{if .Contracts}}
- 修改契约路径后，同时读取相关前端、后端、中间件或 SDK 子项目 skill，并检查生成客户端、服务端处理器、中间件适配器和测试。
{{end}}
{{if .Shared}}
- 修改共享路径后，同时读取所有导入、打包或生成该共享代码的子项目 skill。
{{end}}
{{if .Infra}}
- 修改基础设施路径后，同时读取受部署、配置、网关或中间件规则影响的子项目 skill。
{{end}}

## 并发 Agent 约束

- 可以把互不重叠的子项目路径分配给多个 Agent 并发处理。
- 每个 Agent 必须只写自己的子项目路径；共享路径、契约路径和基础设施路径应由单个 Agent 先处理。
- 契约或共享接口稳定后，消费者子项目才能并发跟进。
- 合并并发结果前，统一检查接口形状、错误码、环境变量、生成物和测试是否一致。
