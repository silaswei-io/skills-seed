# 跨项目规则

本文件用于判断跨项目影响、安排改动顺序和限制并发写入。子项目内部规则仍以对应子项目 skill 为准。

## 基本原则

- 先判断影响范围，再写代码；不要因为当前任务从一个路径开始，就假设只影响一个子项目。
- 修改某个子项目代码前，必须读取该子项目的 `SKILL.md` 和 `references/project-spec.md`。
- 修改契约、共享库或部署配置时，先识别生产者、消费者和运行时影响，再读取对应 skills。
- 不要把单个子项目内部约定提升为全局规则，除非多个子项目共同依赖。
- 未配置特殊路径时，使用文件名、目录名和导入关系判断契约、共享和基础设施影响。

## 影响半径

### 单子项目改动

- 只读受影响子项目 skill。
- 写入默认限制在该子项目路径。
- 如果发现需要修改契约、共享代码或部署配置，升级为跨项目改动。

### 契约改动

- 包括 API 定义、OpenAPI/Swagger、proto、请求/响应类型、错误码、事件、消息、生成输入和 SDK。
- 先改契约源，再改生成物或适配层。
- 同时读取生产者和消费者子项目 skill。
- 验证两端字段名、类型、错误码、默认值、兼容性和测试。

### 共享代码改动

- 包括公共类型、公共工具、共享组件、公共配置和跨项目复用库。
- 先稳定公共 API 或函数签名，再更新所有消费者。
- 检查导入方、构建目标和打包产物。
- 如果共享变更破坏兼容性，必须同步更新所有消费者。

### 基础设施改动

- 包括 Dockerfile、Compose、Kubernetes、Helm、Terraform、Nginx、网关、中间件、环境变量、CI 和发布脚本。
- 先确认运行时影响哪些子项目，再读对应 skill。
- 检查端口、路径前缀、健康检查、环境变量名、镜像构建参数、启动命令和发布顺序。

## 改动顺序

1. 列出受影响路径和子项目。
2. 读取工作区概览，确认所有子项目 skill 路径。
3. 如果涉及契约或共享接口，先更新契约源、schema、公共类型或共享函数签名。
4. 更新生产者实现和测试。
5. 更新消费者调用、生成代码、适配层和测试。
6. 更新基础设施、环境变量、网关、中间件或 CI。
7. 汇总验证：生成物、接口形状、错误码、配置键、构建命令和测试范围必须一致。

## 必须同时读取多个 skills 的场景

- 改动路径不在任何单一子项目内。
- 改动会改变 API、类型、schema、错误码、事件、消息或生成输入。
- 改动共享库、公共工具、公共组件或公共配置。
- 改动部署、网关、中间件、CI、环境变量或发布脚本。
- 改动根目录脚本、workspace 级 package/go work/build 配置或批量命令。
{{if .HasContracts}}
- 修改已配置契约路径时，同时读取相关生产者、消费者、中间件或 SDK 子项目 skill。
{{end}}
{{if .HasShared}}
- 修改已配置共享路径时，同时读取所有导入、打包或生成该共享代码的子项目 skill。
{{end}}
{{if .HasInfra}}
- 修改已配置基础设施路径时，同时读取受部署、配置、网关或中间件规则影响的子项目 skill。
{{end}}

## 并发 Agent 约束

- 可以并发：互不重叠的子项目内部改动，且每个 Agent 只写自己的子项目路径。
- 不可并发：同一契约文件、同一共享文件、同一基础设施文件、根配置和工作区级脚本。
- 并发前必须明确每个 Agent 的读写边界和受影响子项目。
- 契约或共享接口稳定前，消费者子项目不能并发实现。
- 合并并发结果前，统一检查接口形状、错误码、环境变量、生成物、构建命令和测试是否一致。

## 输出要求

- 说明本次任务命中的子项目和路径。
- 说明是否涉及契约、共享代码或基础设施。
- 说明读取了哪些子项目 skill。
- 对跨项目任务，说明改动顺序和验证范围。
