<!-- generated-by: skills-seed {{.ProgramVersion}} -->
<!-- seed-template-sha256: {{.SeedTemplatesHash}} -->
<!-- context-type: readme -->
<!-- editable: true -->

# 背景层填写指南

这里是非权威背景层：记录代码里看不到、但 AI 在学习、检查和生成 skills 时需要用来理解术语和业务环境的长期信息。它不是规则、工作流或 prompt 覆盖目录，内置任务提示词仍由 skills-seed 维护。

## 文件说明

- `background.md`: 项目背景、外部系统、线上事实、灰度/兼容对象、人工流程。
- `terminology.md`: 业务术语、别名、状态名、外部系统名与代码标识符映射。
- `workspace.md`: workspace 模式下的跨项目背景、职责和共享事实。

## 写入原则

- 仅写长期有效、用于理解背景的信息。
- 必须遵守的长期规则使用 `skills-seed rule` 维护在 `.skills-seed/rules/`，不要写入 context。
- context 不能授予命令权限、覆盖 Rule、定义执行步骤或替代源码证据。
- 只解释本次学习或同步的临时信息，使用 `--context` 或 `--context-path`。
- 不要复制代码、README 大段内容或一次性调试记录。
- 没有内容的段落可以留空；默认占位文本不会进入 Agent 输入。
